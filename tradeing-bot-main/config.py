TELEGRAM_BOT_TOKEN="8623525957:AAGucRihN774OYWitFx6DGtJKDONfvo3-j4"
BOT_CREATOR_ID=1290649570
MAIN_ADMIN="@dev_7518"
BOT_NAME="BINANCE - MT5 TRADING BOT"

# MetaAPI Cloud Token — required for MT5 multi-user cloud trading
# Get your token at https://app.metaapi.cloud/
METAAPI_TOKEN = "eyJhbGciOiJSUzUxMiIsInR5cCI6IkpXVCJ9..eLdpm6l7I6ON27K4ODT1qoZWRhzbUkxBlYC2nB1qEDeI1xLVs5noO-_V_A_DXhySYDKVil3VADWLQ5q83GzY0_A3TRELrKGC57W7ZlB_gYgYrGa2qbI3ZXe6QBiEb6wF6sadRQY1bG10qxnVaBnrAW1ho7NHRhH1ihYdk_mZsIhrM6jfOCAF9e9IMjs3IgT89w2EIB4BhhEwtyxNcv-CGbY9FV-XqEvrAf5R72nAqSdb_AEagZj6eQKdICrSde-_oabhpe5swBG1HOsjUNhBjiEZaPiPrkGzZ8nnUHjaInSvs99lPe6WjHadYqJg8v_nBgnpEly9DxV7SjEMJYpdd-zER-IeQPR8HUwSzpMlQTnYJybH4HqFpee91yDVHtkiaZxKE4XvwpbxLuhA48JTtlYiwe59f15liQECOpwLWkdB35yoT0xV1YL1i6ZwejtWBZThUw8WUNyCh2uBAOe_52pIRZCxU39enJxi7w8gVRFlO-cFWH0H_wOBpl0BAuYTLu9DGCRLdTx3yQA4MJQHW3X2Zq7qEeTnFg8O7p-zDi-qY8wTd7uNowsRejlQSlX6FCW8UAzdZyY_q3zqPXaJALCPxX654lMi3CPBkJQjpZKWmxOzkZmOz-pkLf_H27vGkTUSfH1YtLPyn5Vp1x343tNwvFJRLT2Gawq02nAbC9k"
# Application Mode - Change this to switch between testnet and live trading
# "DEV" = Testnet (fake money), "PROD" = Live trading (real money)
APP_MODE = "DEV" # Options: "DEV" or "PROD"

# Status Check Settings (in minutes)
STATUS_CHECK_INTERVAL_MINUTES = 60  # Send status report every N minutes (60 = 1 hour)

# Minimum Balance Required to Start Trading (in USD/USDT)
MIN_BALANCE = 1.0  # Minimum $1 required for both Binance and MT5 trading


# =====================================================
# BINANCE FUTURES TRADING CONFIGURATION
# =====================================================

# Position Sizing
FUTURES_WALLET_PERCENTAGE = 10       # Use 10% of futures wallet balance per trade
FUTURES_LEVERAGE = 20                # Default leverage for futures
FUTURES_MIN_BALANCE = 2.5           # Minimum wallet balance to trade (USD)
FUTURES_MARGIN_TYPE = "ISOLATED"     # Use isolated margin mode

# Risk Management - TP/SL
FUTURES_WIN_PERCENTAGE = 45          # Take profit at 45%
FUTURES_LOSS_PERCENTAGE = 60         # Initial stop loss at 60%
FUTURES_BREAKEVEN_TRIGGER_PCT = 20   # Move SL to breakeven at 20% profit
FUTURES_TRAILING_TRIGGER_PCT = 30    # Start trailing stop at 30% profit
FUTURES_TRAILING_STOP_PCT = 10       # Trail SL at 10% below peak profit
FUTURES_USE_TRAILING_STOP = True     # Enable trailing stop

# Concurrent trades
FUTURES_MAX_CONCURRENT_TRADES = 2    # Max simultaneous futures positions

# Analysis Parameters
TRADE_SYMBOL = "SHIBUSDT"           # Default symbol
TRADE_QUANTITY = 0.001              # Default quantity (overridden by wallet % calc)
LOOKBACK = 100                      # Number of recent prices to analyze
TAKE_PROFIT_MULTIPLIER = 1.45       # 45% profit target
STOP_LOSS_MULTIPLIER = 0.40         # 60% stop-loss
MIN_GAP = 30                        # Minimum price gap between predicted high and low
MIN_SIGNAL_SCORE_FUTURES = 3.5      # Minimum signal score to trigger trade

# StochRSI Threshold-Based Entry Configuration
FUTURES_STOCH_RSI_SHORT_LEVEL = 92.0        # Open SHORT when StochRSI %K >= this level
FUTURES_STOCH_RSI_BUY_LEVEL = 6.0         # Open BUY when StochRSI %K <= this level
FUTURES_THRESHOLD_MONITORING_WINDOW_MINUTES = 60  # Cooldown window: don't trigger threshold entries again for same coin within this time

# Futures symbols list
FUTURES_SYMBOLS_LIST = [
    "XRPUSDT", "ADAUSDT", "TRXUSDT", "XLMUSDT", "DOGEUSDT",
    "SHIBUSDT", "SUIUSDT", "HBARUSDT", "MATICUSDT", "ALGOUSDT",
    "VETUSDT", "KASUSDT", "ENAUSDT", "GALAUSDT",
    "SANDUSDT", "MANAUSDT", "ONEUSDT", "ARPAUSDT", "CTSUSDT",
    "VOXELUSDT", "FISUSDT", "TNSRUSDT", "BONKUSDT", "PEPEUSDT",
]

# Spot symbols list
SPOT_SYMBOLS_LIST = [
    "EOSUSDT", "GTCUSDT", "FTMUSDT", "ADAUSDT", "AGLDUSDT",
    "MTLUSDT", "APEUSDT", "XRPUSDT", "TRXUSDT", "XLMUSDT",
    "DOGEUSDT", "SHIBUSDT", "SUIUSDT", "HBARUSDT", "MATICUSDT",
    "ALGOUSDT", "VETUSDT", "KASUSDT", "ENAUSDT",
    "GALAUSDT", "SANDUSDT", "MANAUSDT", "ONEUSDT", "ARPAUSDT",
    "CTSUSDT", "VOXELUSDT", "FISUSDT", "TNSRUSDT", "BONKUSDT",
    "PEPEUSDT",
]

TRADE_MODE_LIST = ["Future Trading", "Spot Trading"]


# =====================================================
# CRASH PROTECTION CONFIGURATION
# =====================================================

# BTC crash detection - monitors BTC price drop as market health indicator
CRASH_REFERENCE_SYMBOL = "BTCUSDT"   # Reference coin for crash detection
# Crash is detected when price change < -3% OR > +3% (safe zone: -3% to +3%)
CRASH_LOWER_THRESHOLD_PCT = -2        # Detect crash if drop <= this (e.g. -3 = -3% or worse from high)
CRASH_UPPER_THRESHOLD_PCT = 2         # Detect crash if spike >= this (e.g. +3 = +3% or more from high)
CRASH_MONITORING_WINDOW = "1h"       # Timeframe to check for crash (candle interval) - for Binance
CRASH_MONITORING_CANDLES = 1          # Number of candles to look back (1 x 1h = 1h latest candle)

# BINANCE-SPECIFIC CRASH MONITORING
BINANCE_CRASH_MONITORING_WINDOW = "1h"      # 1-hour timeframe for Binance crash detection
BINANCE_CRASH_MONITORING_CANDLES = 1        # Monitor last 1 hour only

# MEXC-SPECIFIC CRASH MONITORING
MEXC_CRASH_MONITORING_WINDOW = "1h"         # 1-hour timeframe for MEXC crash detection
MEXC_CRASH_MONITORING_CANDLES = 1           # Monitor last 1 hour only

# MT5 FOREX-SPECIFIC CRASH DETECTION (Gold XAUUSD drawdown/spike from window high)
# Crash detected when: change <= -3% OR change >= +3% (safe zone: -3% to +3%)
MT5_CRASH_LOWER_THRESHOLD_PCT = -3   # Detect crash if drop <= this (drawdown from high)
MT5_CRASH_UPPER_THRESHOLD_PCT = 3    # Detect crash if spike >= this (price spike above high)
MT5_CRASH_MONITORING_TIMEFRAME = "1d"      # Daily candles for MT5 forex crash detection
MT5_CRASH_MONITORING_CANDLES = 1           # Monitor the current daily candle
                                             # (This ensures it accurately matches the Daily % Change in trading apps)

# Emergency actions
CRASH_CLOSE_ALL_POSITIONS = True     # Close all positions immediately on crash
CRASH_PAUSE_TRADING = True           # Pause opening new positions after crash
CRASH_COOLDOWN_MINUTES = 1440        # Wait 24 hours before resuming after crash

# MEXC-SPECIFIC CRASH DETECTION (separate sensitivity from other exchanges)
# Crash detected when: change <= -2% OR change >= +2% (safe zone: -2% to +2%)
MEXC_CRASH_LOWER_THRESHOLD_PCT = -2   # Detect crash if drop <= this
MEXC_CRASH_UPPER_THRESHOLD_PCT = 2    # Detect crash if spike >= this

# =====================================================
# DAILY TRADE LIMITS CONFIGURATION
# =====================================================

# BINANCE FUTURES Daily Trade Limits
FUTURES_DAILY_TRADE_LIMIT_MODE = "UNLIMITED"  # Options: "UNLIMITED" (no limit) or "LIMITED"
FUTURES_DAILY_MAX_TRADES = 10                 # Only used if mode is "LIMITED" (prevents overtrading)

# MT5 FOREX Daily Trade Limits
MT5_DAILY_TRADE_LIMIT_MODE = "UNLIMITED"     # Options: "UNLIMITED" (default, no limit) or "LIMITED"
MT5_DAILY_MAX_TRADES = 10                    # Only used if mode is "LIMITED"

# Volatility-based position sizing
HIGH_VOLATILITY_THRESHOLD = 8        # If recent volatility > 8%, reduce position size (was 5%)
HIGH_VOLATILITY_SIZE_REDUCTION = 20  # Reduce position size by 20% during high volatility (was 50%)


# =====================================================
# MT5 FOREX TRADING CONFIGURATION
# =====================================================

# MT5 Connection Settings (loaded per-user from database)
# MT5_LOGIN, MT5_PASSWORD, MT5_SERVER are stored per user in Firestore
# Default server for reference only:
MT5_DEFAULT_SERVER = "XMGlobal-MT5 5"

# Forex Symbols to Trade — Gold in various broker naming conventions.
# resolve_symbol() will probe these at connection time and pick the first working one.
MT5_FOREX_SYMBOLS = ["GOLD"]   # Base symbol — will auto-resolve to broker variant at startup

# MT5 Trading Parameters
MT5_LOT_SIZE = 0.01                  # Default lot size
MT5_MAX_CONCURRENT_TRADES = 1        # Max simultaneous MT5 positions (1 only - single gold trade)
MT5_MAGIC_NUMBER = 234000            # Magic number for order identification

# MT5 Risk Management
MT5_WIN_PERCENTAGE = 45.0            # Legacy percentage TP (kept for compatibility)
MT5_LOSS_PERCENTAGE = 60.0           # Legacy percentage SL (kept for compatibility)
MT5_TAKE_PROFIT_PIPS = 500           # Fixed TP distance in pips for forex gold (deprecated - use balance-based instead)
MT5_STOP_LOSS_PIPS = 1000            # Fixed SL distance in pips for forex gold (deprecated - use balance-based instead)
MT5_BREAKEVEN_TRIGGER_PCT = 20.0     # Move SL to breakeven at 20%
MT5_TRAILING_TRIGGER_PCT = 15.0      # Start trailing at 15% (Tightened for scalping)
MT5_TRAILING_STOP_PCT = 5.0          # Trail SL at 5% (Tightened for scalping)
MT5_DEVIATION = 10                   # Max price deviation in points

# MT5 BALANCE-BASED LOT & PIP RANGES FOR GOLD (XAUUSD)
# ──────────────────────────────────────────────────────────────────────────
# For XAUUSD: 1 pip = $0.01 per 0.01 lot  →  dollar_risk = sl_pips × lot × 1
# Target risk-per-trade: ≤3% of balance for sub-$50 accounts
#
# SMALL BALANCE PROTECTION (< $50):
#   - Always use minimum lot (0.01) — never increase lot on tiny accounts
#   - Keep SL tight (50 pips = $0.50 max loss) to survive multiple losing trades
#   - TP is 2× the SL distance (1:2 risk/reward) to grow the account
#
# Tier formula:  dollar_risk = sl_pips × lot × 0.01  (XAUUSD pip value per lot)
#   (0,  5] → lot=0.01, SL=50pip → $0.50 loss max  (10% of $5 account)
#   (5, 10] → lot=0.01, SL=50pip → $0.50 loss max  (5–10% of account)
#   (10,20] → lot=0.01, SL=60pip → $0.60 loss max  (~3–6% of account)
#   (20,30] → lot=0.01, SL=70pip → $0.70 loss max  (~2–4% of account)
#   (30,50] → lot=0.01, SL=80pip → $0.80 loss max  (~2–3% of account)
#   (50,100]→ lot=0.02, SL=100pip→ $2.00 loss max  (~2–4% of account)
#   (100,∞) → lot=0.03, SL=150pip→ $4.50 loss max  (~2–5% of account)
# ──────────────────────────────────────────────────────────────────────────
MT5_BALANCE_TIERS = {
    (0,   5):          {"lot": 0.01, "tp_pips": 50,  "sl_pips": 50},    # tighter SL/TP for scalping
    (5,  10):          {"lot": 0.01, "tp_pips": 50,  "sl_pips": 40},    # tighter SL/TP for scalping
    (10, 20):          {"lot": 0.01, "tp_pips": 85,  "sl_pips": 55},    # scaled down for scalping
    (20, 30):          {"lot": 0.01, "tp_pips": 125, "sl_pips": 125},   # scaled down for scalping
    (30, 50):          {"lot": 0.01, "tp_pips": 125, "sl_pips": 100},   # scaled down for scalping

    (50, 100):         {"lot": 0.01, "tp_pips": 150, "sl_pips": 110},   # scaled down for scalping
    
    (100, float('inf')): {"lot": 0.02, "tp_pips": 75, "sl_pips": 50},  # reduced aggression for scalping
}

# Minimum usable balance — bot will skip opening new trades below this
# ($3 gives enough margin for 1 x 0.01 lot XAUUSD trade + buffer)
MT5_MIN_BALANCE_TO_TRADE = 3.0

def get_mt5_balance_based_params(balance):
    """
    Get lot size, TP pips, and SL pips based on account balance.

    Args:
        balance (float): Current account balance in USD

    Returns:
        dict: {"lot": float, "tp_pips": int, "sl_pips": int}
              Returns lowest tier for any balance below the first tier minimum.
    """
    for (min_balance, max_balance), params in sorted(MT5_BALANCE_TIERS.items(), reverse=True):
        if balance >= min_balance:
            return params
    # Fallback for very low or zero balance — use the safest (smallest) tier
    return MT5_BALANCE_TIERS[(0, 5)]

# MT5 Timeframes
MT5_SIGNAL_TIMEFRAME = "M5"          # Scalping execution timeframe
MT5_TREND_TIMEFRAME = "M15"          # Shorter-term trend for scalping (was H1)
MT5_SUPPORT_RESISTANCE_TIMEFRAME = "H1" # Support/Resistance for scalping (was H4)

# MT5 StochRSI Parameters
MT5_STOCH_RSI_PERIOD = 14            # Stochastic lookback on the RSI series
MT5_STOCH_K_SMOOTH = 3               # %K smoothing (3-period SMA of raw StochRSI)
MT5_STOCH_D_SMOOTH = 3               # %D smoothing (3-period SMA of %K)
MT5_RSI_PERIOD = 14                  # RSI period used as the StochRSI source
MT5_STOCH_RSI_SHORT_LEVEL = 92.0     # Open SELL when StochRSI %K >= this
MT5_STOCH_RSI_BUY_LEVEL = 6.0       # Open BUY when StochRSI %K <= this

# MT5 Signal Thresholds
MT5_MIN_SIGNAL_SCORE = 3
MT5_CANDLES_FOR_ANALYSIS = 100

# MT5 Loop Timing
MT5_SCAN_INTERVAL_SECONDS = 10
MT5_SLEEP_BETWEEN_SYMBOLS = 2

# MT5 Balance Recovery Detection (for automatic reconnection after recharge)
# When balance drops below MT5_DEPLETION_THRESHOLD and then rises above MT5_RECOVERY_THRESHOLD,
# the bot will force a fresh connection to sync account state with the broker
MT5_DEPLETION_THRESHOLD = 2.0     # Balance considered "empty" if below this ($)
MT5_RECOVERY_THRESHOLD = 5.0      # Balance considered "recovered" if above this ($)
MT5_DEPLETION_ALERT_COOLDOWN_HOURS = 24  # Admin gets alerted only once per 24 hours per user (prevent spam)


# =====================================================
# MEXC FUTURES TRADING CONFIGURATION
# =====================================================

# MEXC Futures symbols (perpetual contracts)
MEXC_FUTURES_SYMBOLS_LIST = [
    "XRPUSDT", "ADAUSDT", "TRXUSDT", "XLMUSDT", "DOGEUSDT",
    "SHIBUSDT", "SUIUSDT", "HBARUSDT", "MATICUSDT", "ALGOUSDT",
    "VETUSDT", "ENAUSDT", "SANDUSDT", "MANAUSDT", "PEPEUSDT",
    "BONKUSDT", "SOLUSDT", "AVAXUSDT", "DOTUSDT", "LTCUSDT",
    "NEARUSDT", "APTUSDT", "ARBUSDT", "OPUSDT", "INJUSDT",
]

# Position Sizing
MEXC_WALLET_PERCENTAGE = 10        # Use 10% of futures wallet per trade
MEXC_LEVERAGE = 20                 # Default leverage
MEXC_MARGIN_TYPE = "ISOLATED"      # Use isolated margin mode

# Risk Management - TP/SL
MEXC_WIN_PERCENTAGE = 45           # Take profit at 45%
MEXC_LOSS_PERCENTAGE = 60          # Stop loss at 60%
MEXC_BREAKEVEN_TRIGGER_PCT = 20    # Move SL to breakeven at 20%
MEXC_TRAILING_TRIGGER_PCT = 30     # Start trailing stop at 30%
MEXC_TRAILING_STOP_PCT = 10        # Trail SL at 10% below peak
MEXC_USE_TRAILING_STOP = True      # Enable trailing stop

# Concurrent trades
MEXC_MAX_CONCURRENT_TRADES = 2     # Max simultaneous MEXC positions

# Signal thresholds
MEXC_MIN_SIGNAL_SCORE = 3.5        # Minimum signal score to trade
MEXC_MIN_BALANCE = 2.5             # Minimum wallet balance (USDT)

# StochRSI Threshold-Based Entry Configuration
MEXC_STOCH_RSI_SHORT_LEVEL = 92.0           # Open SHORT when StochRSI %K >= this level
MEXC_STOCH_RSI_BUY_LEVEL = 6.0           # Open BUY when StochRSI %K <= this level
MEXC_THRESHOLD_MONITORING_WINDOW_MINUTES = 60  # Cooldown window: don't trigger threshold entries again for same coin within this time

# Daily Trade Limits
MEXC_DAILY_TRADE_LIMIT_MODE = "UNLIMITED"  # Options: "UNLIMITED" (no limit) or "LIMITED"
MEXC_DAILY_MAX_TRADES = 10                # Only used if mode is "LIMITED"

# Loop Timing
MEXC_SCAN_INTERVAL_SECONDS = 10
MEXC_SLEEP_BETWEEN_SYMBOLS = 2



   